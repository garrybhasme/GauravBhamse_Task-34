# GauravBhamse_Task(34)

This is Task no.34 

#Task:{
  id:unique_id,
  text:content,
  actions:{
    complete:bool,
    edit:bool,
    delete:bool
  }
}

Steps performed to crate project:
npm init: to start application
npm i express: install express
npm i nodemon --save-dev: install nodemon